import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: 100,
        height: 50,
        backgroundColor: 'red'
    }
})

export default styles;